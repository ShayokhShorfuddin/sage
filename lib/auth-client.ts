import { createAuthClient } from 'better-auth/react';
// TODO: We skipped adding baseURl here. Maybe try adding if anything breaks. https://www.better-auth.com/docs/installation#create-client-instance
export const authClient = createAuthClient({});
